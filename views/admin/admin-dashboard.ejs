<%- include('./partials/header', { pageTitle: 'Dashboard' }) %>

<%- include('./partials/sidebar', { page: 'dashboard' }) %>

<div class="main-content">
    <!-- Top Header -->
    <header class="top-header">
        <div class="header-content">
            <div class="header-left">
                <button class="sidebar-toggle" onclick="toggleSidebar()">
                    <i class="fas fa-bars"></i>
                </button>
                <div>
                    <h1 class="page-title">Dashboard</h1>
                    <div class="breadcrumbs">
                        <span>Overview</span>
                    </div>
                </div>
            </div>
            <div class="header-right">
                <div class="admin-profile">
                    <div class="admin-avatar">
                        A
                    </div>
                    <div class="admin-info">
                        <div class="admin-name">Admin</div>
                        <div class="admin-role">Administrator</div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Content Area -->
    <div class="content-area">
        <!-- Stats Cards -->
        <div class="row g-4 mb-4">
            <div class="col-xl-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-icon stats-icon-primary">
                        <i class="fas fa-users"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-number"><%= typeof totalUsers !== 'undefined' ? totalUsers : 0 %></div>
                        <div class="stats-label">Total Customers</div>
                        <div class="stats-change stats-change-positive">
                            <i class="fas fa-arrow-up"></i>
                            <span>+5.2% from last month</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-icon stats-icon-success">
                        <i class="fas fa-rupee-sign"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-number">₹<%= typeof totalRevenue !== 'undefined' ? totalRevenue.toLocaleString('en-IN', { maximumFractionDigits: 0 }) : 0 %></div>
                        <div class="stats-label">Total Revenue</div>
                        <div class="stats-change stats-change-positive">
                            <i class="fas fa-arrow-up"></i>
                            <span>+12.5% from last month</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-icon stats-icon-warning">
                        <i class="fas fa-shopping-cart"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-number"><%= typeof totalOrders !== 'undefined' ? totalOrders : 0 %></div>
                        <div class="stats-label">Total Orders</div>
                        <div class="stats-change stats-change-positive">
                            <i class="fas fa-arrow-up"></i>
                            <span>+8.1% from last month</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-xl-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-icon stats-icon-info">
                        <i class="fas fa-box"></i>
                    </div>
                    <div class="stats-content">
                        <div class="stats-number"><%= typeof totalProducts !== 'undefined' ? totalProducts : 0 %></div>
                        <div class="stats-label">Total Products</div>
                        <div class="stats-change stats-change-negative">
                            <i class="fas fa-arrow-down"></i>
                            <span>-2.3% from last month</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Charts and Tables Row -->
        <div class="row g-4 mb-4">
            <!-- Sales Chart -->
            <div class="col-xl-8">
                <div class="card-modern no-hover">
                    <div class="card-header-modern">
                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="card-title mb-0">Sales Overview</h5>
                            <div class="dropdown">
                                <button class="btn btn-ghost-modern btn-sm dropdown-toggle" id="chartFilterBtn" type="button" data-bs-toggle="dropdown">
                                    Last 7 days
                                </button>
                                <ul class="dropdown-menu dropdown-menu-modern">
                                    <li><a class="dropdown-item chart-filter" data-period="daily" href="#">Last 7 days</a></li>
                                    <li><a class="dropdown-item chart-filter" data-period="monthly" href="#">Monthly (12 months)</a></li>
                                    <li><a class="dropdown-item chart-filter" data-period="yearly" href="#">Yearly (5 years)</a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="card-body-modern">
                        <div class="chart-container">
                            <canvas id="salesChart" height="300"></canvas>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Order Status Distribution -->
            <div class="col-xl-4">
                <div class="card-modern no-hover">
                    <div class="card-header-modern">
                        <h5 class="card-title mb-0">Order Status</h5>
                    </div>
                    <div class="card-body-modern">
                        <div class="order-status-list">
                            <div class="status-item">
                                <div class="status-info">
                                    <span class="status-badge status-pending">
                                        <i class="fas fa-circle"></i>
                                        Pending
                                    </span>
                                    <span class="status-count"><%= statusCounts && statusCounts.pending || 0 %></span>
                                </div>
                                <div class="status-progress">
                                    <div class="progress progress-modern">
                                        <div class="progress-bar progress-bar-modern" style="width: <%= statusPercents && statusPercents.pending || 0 %>%"></div>
                                    </div>
                                </div>
                            </div>

                            <div class="status-item">
                                <div class="status-info">
                                    <span class="status-badge status-processing">
                                        <i class="fas fa-circle"></i>
                                        Processing
                                    </span>
                                    <span class="status-count"><%= statusCounts && statusCounts.processing || 0 %></span>
                                </div>
                                <div class="status-progress">
                                    <div class="progress progress-modern">
                                        <div class="progress-bar progress-bar-modern" style="width: <%= statusPercents && statusPercents.processing || 0 %>%"></div>
                                    </div>
                                </div>
                            </div>

                            <div class="status-item">
                                <div class="status-info">
                                    <span class="status-badge status-shipped">
                                        <i class="fas fa-circle"></i>
                                        Shipped
                                    </span>
                                    <span class="status-count"><%= statusCounts && statusCounts.shipped || 0 %></span>
                                </div>
                                <div class="status-progress">
                                    <div class="progress progress-modern">
                                        <div class="progress-bar progress-bar-modern" style="width: <%= statusPercents && statusPercents.shipped || 0 %>%"></div>
                                    </div>
                                </div>
                            </div>

                            <div class="status-item">
                                <div class="status-info">
                                    <span class="status-badge status-delivered">
                                        <i class="fas fa-circle"></i>
                                        Delivered
                                    </span>
                                    <span class="status-count"><%= statusCounts && statusCounts.delivered || 0 %></span>
                                </div>
                                <div class="status-progress">
                                    <div class="progress progress-modern">
                                        <div class="progress-bar progress-bar-modern" style="width: <%= statusPercents && statusPercents.delivered || 0 %>%"></div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Recent Activity and Top Products -->
        <div class="row g-4 mb-4">
            <!-- Recent Orders -->
            <div class="col-xl-8">
                <div class="card-modern no-hover">
                    <div class="card-header-modern">
                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="card-title mb-0">Recent Orders</h5>
                            <a href="/admin/orders" class="btn btn-outline-modern btn-sm">
                                View All
                                <i class="fas fa-arrow-right ms-2"></i>
                            </a>
                        </div>
                    </div>
                    <div class="card-body-modern p-0">
                        <div class="table-responsive">
                            <table class="table table-hover mb-0 table-dashboard">
                                <thead class="table-header-modern">
                                    <tr>
                                        <th>Order ID</th>
                                        <th>Customer</th>
                                        <th>Status</th>
                                        <th>Amount</th>
                                        <th>Date</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (recentOrders && recentOrders.length) { %>
                                        <% recentOrders.forEach(function(o){ %>
                                            <tr>
                                                <td>
                                                    <span class="fw-medium order-id-dashboard">#<%= o.orderID %></span>
                                                </td>
                                                <td>
                                                    <div class="d-flex align-items-center gap-2">
                                                        <div class="user-avatar-sm"><%= (o.user && o.user.name ? o.user.name.charAt(0).toUpperCase() : 'U') %></div>
                                                        <span><%= o.user && o.user.name ? o.user.name : 'Unknown' %></span>
                                                    </div>
                                                </td>
                                                <td>
                                                    <span class="status-badge status-<%= (o.orderStatus || '').replace(/\s+/g, '-') %>">
                                                        <i class="fas fa-circle"></i>
                                                        <%= o.orderStatus %>
                                                    </span>
                                                </td>
                                                <td class="amount-dashboard">₹<%= (o.finalAmount || 0).toLocaleString('en-IN', { maximumFractionDigits: 0 }) %></td>
                                                <td class="text-muted-modern"><%= new Date(o.createdAt).toLocaleString('en-IN', { hour: '2-digit', minute: '2-digit', day: '2-digit', month: 'short' }) %></td>
                                            </tr>
                                        <% }) %>
                                    <% } else { %>
                                        <tr><td colspan="5" class="text-center text-muted-modern py-3">No recent orders</td></tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Right Column for Top Products and Categories -->
            <div class="col-xl-4">
                <div class="d-flex flex-column gap-4">
                    <!-- Top Products -->
                    <div class="card-modern no-hover">
                        <div class="card-header-modern">
                            <h5 class="card-title mb-0">Top Products</h5>
                        </div>
                        <div class="card-body-modern">
                            <div class="top-products-list">
                                <% if (topProducts && topProducts.length) { %>
                                    <% topProducts.forEach(function(p){ %>
                                        <div class="product-item">
                                            <div class="product-info">
                                                <div class="product-image">
                                                    <i class="fas fa-box"></i>
                                                </div>
                                                <div class="product-details">
                                                    <div class="product-name"><%= p.name %></div>
                                                    <div class="product-sales"><%= p.quantity %> sold</div>
                                                </div>
                                            </div>
                                            <div class="product-revenue">₹<%= (p.revenue || 0).toLocaleString('en-IN', { maximumFractionDigits: 0 }) %></div>
                                        </div>
                                    <% }) %>
                                <% } else { %>
                                    <div class="text-center text-muted-modern py-3">No product sales data</div>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <!-- Top Categories -->
                    <div class="card-modern no-hover">
                        <div class="card-header-modern">
                            <h5 class="card-title mb-0">Top Selling Categories</h5>
                        </div>
                        <div class="card-body-modern">
                            <div class="top-products-list">
                                <% if (topCategories && topCategories.length) { %>
                                    <% topCategories.forEach(function(cat){ %>
                                        <div class="product-item">
                                            <div class="product-info">
                                                <div class="product-image" style="background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);">
                                                    <i class="fas fa-tags"></i>
                                                </div>
                                                <div class="product-details">
                                                    <div class="product-name"><%= cat.name %></div>
                                                    <div class="product-sales"><%= cat.totalSold %> sold</div>
                                                </div>
                                            </div>
                                            <div class="product-revenue">₹<%= (cat.totalRevenue || 0).toLocaleString('en-IN', { maximumFractionDigits: 0 }) %></div>
                                        </div>
                                    <% }) %>
                                <% } else { %>
                                    <div class="text-center text-muted-modern py-3">No category sales data</div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>


/* Enhance alignment for Top Products and Categories */
.col-xl-4 .d-flex {
    min-height: 100%;
}

.card-modern {
    display: flex;
    flex-direction: column;
    height: 100%;
}

.card-body-modern {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
}

.top-products-list {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.product-item {
    flex: 1 1 auto;
    min-height: 0;
}

@media (min-width: 1200px) {
    .col-xl-4 .d-flex {
        flex-direction: row;
        gap: 1.5rem;
    }

    .card-modern {
        width: 100%;
    }

    .card-modern.flex-grow-1 {
        flex: 1;
    }
}

@media (max-width: 1199px) {
    .col-xl-4 .d-flex {
        flex-direction: column;
        gap: 1rem;
    }

    .card-modern {
        width: 100%;
    }
}
    /* Modern Purple Theme Styles for Dashboard */
    
    /* Stats Cards */
    .stats-card {
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 16px;
        padding: 1.5rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        transition: all 0.3s ease;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        position: relative;
        overflow: hidden;
    }

    .stats-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .stats-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
    }

    .stats-icon {
        width: 60px;
        height: 60px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        color: #ffffff;
        flex-shrink: 0;
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .stats-icon-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .stats-icon-success {
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }

    .stats-icon-warning {
        background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }

    .stats-icon-info {
        background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
    }

    .stats-content {
        flex: 1;
    }

    .stats-number {
        font-size: 1.75rem;
        font-weight: 700;
        color: #0f172a;
        line-height: 1.2;
        font-family: 'Inter', sans-serif;
    }

    .stats-label {
        font-size: 0.875rem;
        color: #64748b;
        font-weight: 500;
        margin-bottom: 0.25rem;
    }

    .stats-change {
        font-size: 0.75rem;
        display: flex;
        align-items: center;
        gap: 0.25rem;
        font-weight: 500;
    }

    .stats-change-positive {
        color: #059669;
    }

    .stats-change-negative {
        color: #dc2626;
    }

    /* Card Modern Styles */
    .card-modern {
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 16px;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        overflow: hidden;
    }

    .card-modern.no-hover {
        transition: none;
    }

    .card-header-modern {
        padding: 1.5rem 2rem;
        border-bottom: 1px solid #e2e8f0;
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
    }

    .card-title {
        font-size: 1.125rem;
        font-weight: 600;
        color: #0f172a;
        margin: 0;
    }

    .card-body-modern {
        padding: 2rem;
    }

    .card-body-modern.p-0 {
        padding: 0;
    }

    /* Button Styles */
    .btn-ghost-modern {
        background: transparent;
        color: #64748b;
        border: 2px solid #e2e8f0;
        padding: 0.5rem 1rem;
        border-radius: 8px;
        font-size: 0.875rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
    }

    .btn-ghost-modern:hover {
        background: #f8fafc;
        border-color: #cbd5e1;
        color: #475569;
    }

    .btn-outline-modern {
        background: transparent;
        color: #667eea;
        border: 2px solid #667eea;
        padding: 0.5rem 1rem;
        border-radius: 8px;
        font-size: 0.875rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
    }

    .btn-outline-modern:hover {
        background: #667eea;
        color: #ffffff;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    /* Chart Container */
    .chart-container {
        position: relative;
        height: 300px;
        width: 100%;
    }

    /* Order Status List */
    .order-status-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .status-item {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .status-info {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .status-count {
        font-weight: 600;
        color: #0f172a;
        font-family: 'Inter', sans-serif;
    }

    .status-progress {
        width: 100%;
    }

    .progress-modern {
        height: 8px;
        background-color: #f1f5f9;
        border-radius: 4px;
        overflow: hidden;
    }

    .progress-bar-modern {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 4px;
        transition: width 0.6s ease;
        height: 100%;
    }

    /* Status Badges */
    .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.375rem;
        padding: 0.375rem 0.75rem;
        border-radius: 50px;
        font-size: 0.75rem;
        font-weight: 500;
        text-transform: capitalize;
    }

    .status-badge i {
        font-size: 0.5rem;
    }

    .status-pending {
        background: rgba(251, 191, 36, 0.1);
        color: #d97706;
        border: 1px solid rgba(251, 191, 36, 0.2);
    }

    .status-processing {
        background: rgba(59, 130, 246, 0.1);
        color: #2563eb;
        border: 1px solid rgba(59, 130, 246, 0.2);
    }

    .status-shipped {
        background: rgba(102, 126, 234, 0.1);
        color: #667eea;
        border: 1px solid rgba(102, 126, 234, 0.2);
    }

    .status-delivered {
        background: rgba(16, 185, 129, 0.1);
        color: #059669;
        border: 1px solid rgba(16, 185, 129, 0.2);
    }

    /* User Avatar */
    .user-avatar-sm {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #ffffff;
        font-weight: 600;
        font-size: 0.75rem;
    }

    /* Table Styles */
    .table-header-modern {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
    }

    .table-dashboard th {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
        border: none;
        padding: 1rem 1.5rem;
        font-weight: 600;
        font-size: 0.875rem;
        color: #374151;
        border-bottom: 1px solid #e2e8f0;
    }

    .table-dashboard td {
        padding: 1rem 1.5rem;
        border: none;
        border-bottom: 1px solid #f1f5f9;
        font-size: 0.875rem;
        color: #0f172a;
    }

    .table-dashboard tbody tr:last-child td {
        border-bottom: none;
    }

    .table-dashboard tbody tr:hover {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
    }

    .order-id-dashboard {
        color: #667eea;
        font-family: 'Inter', sans-serif;
        font-weight: 600;
    }

    .amount-dashboard {
        font-weight: 600;
        color: #0f172a;
        font-family: 'Inter', sans-serif;
    }

    .text-muted-modern {
        color: #64748b;
        font-size: 0.875rem;
    }

    /* Top Products */
    .top-products-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .product-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem;
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        transition: all 0.3s ease;
    }

    .product-item:hover {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.2);
        transform: translateY(-2px);
    }

    .product-info {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        flex: 1;
    }

    .product-image {
        width: 48px;
        height: 48px;
        border-radius: 12px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #ffffff;
        font-size: 1.125rem;
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .product-details {
        flex: 1;
    }

    .product-name {
        font-weight: 600;
        color: #0f172a;
        font-size: 0.875rem;
        line-height: 1.2;
        margin-bottom: 0.25rem;
    }

    .product-sales {
        font-size: 0.75rem;
        color: #64748b;
        font-weight: 500;
    }

    .product-revenue {
        font-weight: 700;
        color: #0f172a;
        font-size: 0.875rem;
        font-family: 'Inter', sans-serif;
    }

    /* Dropdown Styles */
    .dropdown-menu-modern {
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        box-shadow: 0 10px 40px -12px rgba(0, 0, 0, 0.25);
        padding: 0.5rem;
    }

    .dropdown-menu-modern .dropdown-item {
        color: #374151;
        padding: 0.5rem 1rem;
        border-radius: 8px;
        transition: all 0.3s ease;
        font-weight: 500;
    }

    .dropdown-menu-modern .dropdown-item:hover {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
        color: #667eea;
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
        .stats-card {
            padding: 1rem;
        }

        .stats-icon {
            width: 50px;
            height: 50px;
            font-size: 1.25rem;
        }

        .stats-number {
            font-size: 1.5rem;
        }

        .chart-container {
            height: 250px;
        }

        .card-header-modern,
        .card-body-modern {
            padding: 1rem 1.5rem;
        }

        .product-item {
            padding: 0.75rem;
        }

        .product-image {
            width: 40px;
            height: 40px;
            font-size: 1rem;
        }
    }

    @media (max-width: 576px) {
        .stats-card {
            flex-direction: column;
            text-align: center;
            gap: 0.75rem;
        }

        .stats-content {
            text-align: center;
        }

        .card-header-modern,
        .card-body-modern {
            padding: 1rem;
        }

        .product-item {
            flex-direction: column;
            gap: 0.75rem;
            text-align: center;
        }

        .product-info {
            flex-direction: column;
            text-align: center;
        }
    }
</style>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const ctx = document.getElementById('salesChart');
        let chartInstance = null;
        const filterBtn = document.getElementById('chartFilterBtn');

        async function loadChart(period = 'daily') {
            try {
                const resp = await fetch(`/admin/dashboard/chart-data?period=${encodeURIComponent(period)}`, {
                    headers: { 'Accept': 'application/json' },
                    credentials: 'include'
                });
                const result = await resp.json();
                if (!result.success) return;

                const labels = result.labels;
                const data = result.data;

                const config = {
                    type: 'line',
                    data: {
                        labels,
                        datasets: [{
                            label: 'Sales',
                            data,
                            borderColor: '#667eea',
                            backgroundColor: 'rgba(102, 126, 234, 0.1)',
                            borderWidth: 3,
                            fill: true,
                            tension: 0.4,
                            pointBackgroundColor: '#667eea',
                            pointBorderColor: '#ffffff',
                            pointBorderWidth: 2,
                            pointRadius: 4,
                            pointHoverRadius: 6,
                            pointHoverBackgroundColor: '#764ba2',
                            pointHoverBorderColor: '#ffffff'
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: { legend: { display: false } },
                        scales: {
                            y: {
                                beginAtZero: true,
                                grid: { color: '#e2e8f0' },
                                ticks: {
                                    color: '#64748b',
                                    callback: function(value) { return '₹' + (value / 1000) + 'k'; }
                                }
                            },
                            x: { grid: { display: false }, ticks: { color: '#64748b' } }
                        }
                    }
                };

                if (chartInstance) {
                    chartInstance.destroy();
                }
                chartInstance = new Chart(ctx, config);

                // Update filter button text
                if (filterBtn) {
                    if (period === 'yearly') filterBtn.textContent = 'Yearly (5 years)';
                    else if (period === 'monthly') filterBtn.textContent = 'Monthly (12 months)';
                    else filterBtn.textContent = 'Last 7 days';
                }
            } catch (e) {
                console.error('Chart load error:', e);
            }
        }

        // Initial load
        if (ctx) loadChart('daily');

        // Bind filter clicks
        document.querySelectorAll('.chart-filter').forEach(a => {
            a.addEventListener('click', (e) => {
                e.preventDefault();
                const period = a.dataset.period || 'daily';
                loadChart(period);
            });
        });
    });
</script>

<%- include('./partials/footer') %>